export type Activity = {
    id: string,
    time?: string,
    duration?: string,
    image?: string,
    title: string,
    description: string,
    type: string,
    link?: string,
    photos?: string[],
    price?: number,
}